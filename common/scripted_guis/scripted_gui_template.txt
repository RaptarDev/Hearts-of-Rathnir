scripted_gui = {
  countrymarketview = {
    window_name = "countrymarketview"
    context_type = player_context

    visible = {
      has_country_flag = market_open
    }

    effects = {
      close_button_click = {
        clr_country_flag = market_open
      }
        
    }

    triggers = {
    }

    dynamic_lists = {
	  }



    ai_enabled = {
	  	always = no
    }

    ai_test_interval = 290  # The higher, the long it takes the ai to do actions
	  ai_test_variance = 0.5

    ai_check = {
    }

    ai_check_scope = {
    }

    ai_weights = {
    }
  }
  mercwindow = {
    window_name = "sortbyproduct"
    context_type = player_context
    parent_window_token = "countrymarketview"
    effects = {
  		merc_button_click = {
        set_country_flag = open_merc
        #set_country_flag = missile_production_type_1
        set_prod_display_arrays = yes
      }
    }
    properties = {
      merc_button = {
        if = {
          limit = { has_country_flag = open_merc }
          frame = 2
        }
			}
    }

  }

}
